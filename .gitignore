# go
/bin/
/out/
*.test
*.out
.env
.env.*

# stray `go build ./tier2/<x>` / `go run` binaries land at repo root named after
# the package dir — never commit them
/amazon
/apple
/ashby
/example
/greenhouse
/lever
/scraper
/workday

# per-module compiled binaries (go build . inside a module dir)
modules/*/greenhouse
modules/*/lever
modules/*/ashby
modules/*/workday
modules/*/amazon
modules/*/apple
